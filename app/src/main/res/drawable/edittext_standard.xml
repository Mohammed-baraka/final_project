<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- لون الخلفية -->
    <solid android:color="@android:color/white" />

    <!-- زوايا مستديرة -->
    <corners android:radius="12dp" />

    <!-- حواف الحقل -->
    <stroke
        android:width="2dp"
        android:color="@color/teal_500" />

    <!-- حواف داخلية (Padding داخلي) -->
    <padding
        android:left="8dp"
        android:right="8dp"
        android:top="8dp"
        android:bottom="8dp" />
</shape>
